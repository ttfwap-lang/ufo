# Project Status

> **Target format: auto-generated by CI on every successful run.** Phase 6 (CI/CD) hasn't landed yet, so this file is currently **hand-maintained** in the shape CI will eventually produce — update it by hand until then, and don't trust the "Generated" timestamp below as a real CI run.

## Current State (Track A: Phases 0-10)

| Phase | Focus | Status | Verification |
|-------|-------|--------|--------------|
| 0 | DGX Reality Audit | ✅ Complete | `dgx_audit.json` confirmed Ollama `:11434` + vLLM `:8000` |
| 1 | DGX Config & Probes | ✅ Complete (2026-09-18) | `terminal.py`/`terminal_config.py` repointed to `:11434`/`:8000`; `check_dgx_host()` probes `/api/tags`/`/v1/models`; menu/status text fixed; `--selftest` passes |
| 2 | Windows Automation (Hybrid) | 🔄 In Progress (2026-09-18) | `automation/` package built (`DesktopAutomation` protocol, `PlaywrightDesktop`, `UIADesktop`, factory); 18 mocked unit tests pass. NOT verified against a real browser/Windows GUI (none available here). 14 pywinauto/uiautomation call sites found (not 16); 2 annotated, 12 untouched pending per-call-site migration |
| 3 | Packaging & Dependency Hygiene | ✅ Complete (2026-09-18) | `pyproject.toml` `[project]`/`[build-system]`/scripts sections added; `uv lock` succeeded (212 packages resolved) |
| 4 | Security & Secrets Hardening | 🔄 Mostly done (2026-09-18) | `dgx_audit.json`/`pr_diff_review.txt` gitignored; both `Machine`-scope env vars → `User` scope; both HKLM reads replaced by new `config/secrets.py`; Gemini key moved out of URL into a header; PID validation added before `taskkill`. Still open: `litellm_config.yaml`'s hardcoded Tailscale IP (real design decision, not a quick fix) |
| 5 | Eliminate `NotImplementedError` Stubs | 🔄 In Progress | `llm/llm_call.py`'s import-time blocker fixed (see #0 below). Remaining stub fixes in progress in parallel |
| 6 | CI/CD | 🔄 In Progress | `.github/workflows/ci.yml` now exists (`lint`, `unit-tests`, `windows-tests`, `dgx-integration`); never actually run (no GitHub remote trigger in this environment). Self-hosted runners still not provisioned — `windows-tests`/`dgx-integration` stay `if: false` |
| 7 | Terminal Widget Modularization | ⏳ Pending | `scripts/terminal.py` (~1,220 lines) not yet split into a package |
| 8 | Galaxy DGX Device Agent | 🔄 Mostly done (2026-09-18) | `galaxy/device_agents/dgx_device_agent.py` built against verified real `RegistrationProtocol`/`WebSocketTransport`/`TaskExecutionProtocol` signatures; routes vision tasks to Ollama, text to vLLM; `devices.yaml` entry + `DeviceType.DGX_SPARK` added. NOT live-tested (no Galaxy server/DGX host here) and mocked unit tests could not even be collected — `galaxy/__init__.py`'s eager import chain pulls in torch/pandas/chromadb/etc. not installed here (pre-existing Phase 3/13 problem, not caused by this phase) |
| 9 | Documentation Overhaul | 🔄 In Progress | `status.md`/`testing.md`/`architecture.md` + 3 deployment guides (`docs/deployment/*.md`) now exist (hand-authored, not yet CI-generated — Phase 6 blocked) |
| 10 | Golden Path E2E Tests | ⏳ Pending | Blocked on Phase 5 (Linux session) finishing; Phase 3 no longer blocks it |

## Track B (Phases 11-15) — Gated on Track A Green + Soak

| Phase | Focus | Status |
|-------|-------|--------|
| 11 | DI Container for Config Resolution | ⏳ Blocked |
| 12 | Unified `LLMClient` Protocol | ⏳ Blocked |
| 13 | Composition-Based Agents | ⏳ Blocked |
| 14 | Explicit `DeviceAgent` Interface | ⏳ Blocked |
| 15 | Observability & DB Trajectory Store | ⏳ Blocked |

## Current Blocker Summary

0. ~~`llm/llm_call.py` import failure~~ — **FIXED 2026-09-18.** Implemented `dlq/dead_letter_queue.py` (`DeadLetterQueue`, `get_default_dlq()`, `record_dlq_event()`), scoped to LLM-call failures, distinct from `resilience/dlq_manager.py`'s UI-workflow-failure DLQ. All 4 `tests/unit/test_dlq_diagnostic_recorder.py` tests + all 7 `tests/unit/test_circuit_breaker_resilience.py` tests pass; `llm_call.py` imports cleanly.
1. ~~**Phase 3**: `uv lock` failing~~ — **FIXED 2026-09-18.** `uv lock` now succeeds (212 packages resolved).
2. ~~**Phase 1**: DGX ports~~ — **FIXED 2026-09-18.**
3. **Phase 6**: `.github/workflows/ci.yml` now exists but has never run (no GitHub remote trigger here); Windows ARM64 + DGX self-hosted runners still not provisioned
4. **Phase 5**: Linux/Mobile session `evaluation()` + log-save — in progress
5. **Phase 4 (remaining)**: `litellm_config.yaml`'s hardcoded Tailscale IP not yet reconciled with `config/secrets.py`
6. **Phase 2 (remaining)**: 12 of 14 pywinauto/uiautomation-importing files not yet migrated to the new `automation/` protocol; nothing verified against a real Windows GUI/browser
7. **NEW (2026-09-18), found during Phase 8, not fixed (pre-existing, out of Phase 8's scope):** `galaxy/constellation/orchestrator/constellation_manager.py`'s `_get_available_devices()` calls `self._device_manager.device_registry.get_device_info(...)`, but `DeviceRegistry` has no `get_device_info` method (only `get_device`) — this will raise `AttributeError` at runtime today. Separately, `AgentProfile` has no `device_type` field at all, so device-type-based capability matching (including the new `DGX_SPARK` member) is a no-op until that field is threaded through registration. Both are real, pre-existing bugs unrelated to DGX — flagged for whoever picks up Phase 14 or a follow-up fix.
8. **NEW (2026-09-18), found during Phase 8, not fixed (pre-existing, out of scope):** `galaxy/__init__.py` eagerly imports the entire `agents/`→`automator/`→`rag/` chain, requiring `torch`, `pandas`, `chromadb`, `fastmcp`, and others not installed/declared here — this makes `import ufo.galaxy.*` (and therefore any test under `tests/galaxy/`) uncollectable in a minimal environment. This is the exact eager-import problem Phases 3/12/13 already call out; it currently blocks running Phase 8's own new unit tests.

## Fixed Since Last Update (2026-09-18, code-review pass)

13 correctness/efficiency/portability bugs found by `/code-review` against the DGX diff, all fixed and test-verified: uncaught `BackendProfileError` in the EVALUATION-agent fallback (`llm/llm_call.py`), self-referencing LiteLLM DGX fallback chains, a no-op `PYTHON_EXE` fallback in the code-interpreter MCP server, a stale profile cache that ignored `UFO_DGX_HOST` changes, a spurious `DGX_HOST` key leaking into resolved configs, a wrong `sys.path` bootstrap depth in `client/mcp/local_servers/__init__.py`, two tests that crashed instead of skipping on a fresh checkout, duplicated/drifted DGX host normalization, an unnecessary per-call re-scan in the hot config path, an auto-probe cache bypass, 15 desktop launcher scripts with hardcoded personal paths, and a blocking network call inside `async main()`'s startup path. Full list: `docs/plans/E2E_REMEDIATION_PLAN.md`, "Status Update" section at top.

## DGX Backend (Verified Live)

| Service | Port | Model | Role |
|---------|------|-------|------|
| Ollama | 11434 | `gemma4-ufo` (vision) | HOST_AGENT, APP_AGENT, BACKUP_AGENT |
| vLLM | 8000 | `qwen-abliterated` (text) | EVALUATION_AGENT |

**Host**: Tailscale `100.111.170.95` (not `192.168.1.10`; llama-server not running)

## CI Status (Last Run)

| Job | Status | Notes |
|-----|--------|-------|
| lint | ⏳ Configured, never run | `.github/workflows/ci.yml` exists; no GitHub remote trigger in this environment |
| unit-tests | ⏳ Configured, never run | `.github/workflows/ci.yml` exists; no GitHub remote trigger in this environment |
| windows-tests | ❌ Runner missing | Self-hosted `[self-hosted, windows, arm64]` not registered |
| dgx-integration | ❌ Runner missing | Self-hosted `[self-hosted, linux, dgx]` not registered |

---

*Last hand-updated: 2026-09-18 (post code-review fix pass). Not CI-generated — see banner at top.*