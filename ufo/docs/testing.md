# Testing Status

> **Target format: auto-generated by CI on every successful run.** Phase 6 (CI/CD) hasn't landed yet, so this file is currently **hand-maintained** in the shape CI will eventually produce.

## Test Suite Overview

| Test Type | Command | Status | Last Run |
|-----------|---------|--------|----------|
| Unit Tests (Core) | `uv run pytest -q --ignore=tests/integration --ignore=tests/benchmark_harness.py --ignore=tests/load_test_harness.py` | ⏳ Configured, never run | — |
| Windows Automation | `uv run pytest -q automation/tests/` | ❌ Runner missing | — |
| DGX Integration | `uv run pytest -q tests/e2e_golden_path_dgx.py tests/e2e_galaxy_constellation.py` | ❌ Runner missing | — |
| Lint (ruff) | `uv run ruff check .` | ⏳ Configured, never run | — |
| Type Check (mypy) | `uv run mypy .` | ⏳ Configured, never run | — |

## How to Run (2026-09-20)

From the repository root (the folder that contains the `ufo` package):

```bash
ufo/.venv/Scripts/python -m pytest ufo/tests -q --timeout=120     # whole suite
ufo/.venv/Scripts/python -m pytest ufo/tests/unit -q              # fast unit tests
```

Opt-in suites (skipped by default because they use real apps, services or the desktop):

| Suite | How to enable | What it touches |
|-------|---------------|-----------------|
| Live Office COM (`tests/integration/test_office_com_live.py`) | `UFO_LIVE_OFFICE_TESTS=1` | Starts invisible Word/Excel/PowerPoint instances on temp files |
| Live LLM Galaxy (`tests/test_real_galaxy_session_integration.py`) | `UFO_LIVE_LLM_TESTS=1` | Calls the DGX models |
| Live grounding/OmniParser (`tests/integration/test_venus_grounding_live.py`, `test_omniparser_hybrid_live.py`) | none (auto-skips if :8002 / :7861 are unreachable) | DGX vision services through the tunnel |
| Live desktop showcase (`tests/eval_suite/live/`) | run explicitly, see below | Drives the real desktop |

### Live multi-app showcase

```bash
ufo/.venv/Scripts/python -m ufo.tests.eval_suite.live.run_live --list      # the 13 tasks
ufo/.venv/Scripts/python -m ufo.tests.eval_suite.live.run_live --dry-run   # validate, run nothing
ufo/.venv/Scripts/python -m ufo.tests.eval_suite.live.run_live             # drives this desktop
ufo/.venv/Scripts/python -m ufo.tests.eval_suite.live.run_live --tasks N1,W1 --keep
```

Each task runs as a real `python -m ufo` request (retry-until-verified) and is checked
deterministically (file contents, python-docx/openpyxl/python-pptx, PIL, registry, DGX readings).
Output goes to `Desktop\ufo_e2e` (created fresh, deleted unless `--keep`); the report lands in
`logs/live_e2e_<timestamp>/report.md`. Cleanup only closes processes the task itself started —
never a pre-existing Notepad/Office window, and never `explorer.exe`.

## Test Coverage Targets (Track A)

| Phase | Test Requirement | Status |
|-------|------------------|--------|
| 2 | `automation/tests/` — both Playwright + UIA adapters | ⏳ Package not created |
| 3 | `uv sync --all-extras --dry-run` passes | ❌ `uv lock` failing |
| 4 | `bandit -r .` = 0 HIGH/MEDIUM | ⏳ Not run |
| 5 | `grep -r "NotImplementedError" --include="*.py" .` = 0 (source only) | ❌ 14+ stubs remain. **Worse: `import ufo.llm.llm_call` itself fails right now** — `ufo.dlq.dead_letter_queue` doesn't exist (2026-09-18) |
| 7 | `pytest terminal/tests/` passes with mocks | ⏳ Package not created |
| 8 | `DGXDeviceAgent` registration + constellation routing | ⏳ Not implemented |
| 10 | 3 Golden Path E2E tests pass in CI nightly | ⏳ Blocked on Phases 3, 5 |

## Golden Path Tests (Phase 10 — Planned)

| Test | Description | Dependencies |
|------|-------------|--------------|
| `tests/e2e_golden_path_local.py` | Local Ollama → Notepad via Playwright/UIA | Phase 2, 3, 7 |
| `tests/e2e_golden_path_dgx.py` | DGX Ollama/vLLM → vision + text tasks | Phase 1, 3, 5 (Linux session), 8 |
| `tests/e2e_galaxy_constellation.py` | Windows (UI) → DGX (LLM) → Windows (save) | Phase 2, 3, 5, 8 |

## Static Analysis Config (Current `pyproject.toml`)

```toml
[tool.ruff]
target-version = "py310"
line-length = 120
exclude = [".git", "__pycache__", "*.egg-info", "logs"]
select = ["E", "F", "I", "W", "UP", "B", "C4", "SIM"]
ignore = ["E501", "E402"]  # E402 stays until sys.path hacks removed (Cleanup C2)

[tool.mypy]
python_version = "3.10"
ignore_missing_imports = true
follow_imports = "silent"
warn_unused_ignores = true
no_strict_optional = true
exclude = ["logs/", "scripts/legacy/"]
# strict = false globally; per-module overrides added as packages finish (Cleanup C3)
```

## CI Workflow (Implemented, never run — `.github/workflows/ci.yml`)

`.github/workflows/ci.yml` now exists with this shape, but it has never actually executed: this environment has no GitHub remote to push to, so nothing has triggered `push`/`pull_request`. `windows-tests` and `dgx-integration` remain `if: false` pending runner provisioning (see checklist below).

```yaml
jobs:
  lint:
    runs-on: ubuntu-latest
    steps: [checkout, setup-uv, uv sync --all-extras, ruff check, mypy .]

  unit-tests:
    runs-on: ubuntu-latest
    steps: [checkout, setup-uv, uv sync --group core --group eval, pytest -q ...]

  windows-tests:
    runs-on: [self-hosted, windows, arm64]
    if: false  # flip when runner exists
    steps: [checkout, setup-uv, uv sync --group core --group windows --group native-win32 --group eval, playwright install, pytest -q automation/tests/]

  dgx-integration:
    runs-on: [self-hosted, linux, dgx]
    if: false  # flip when runner exists
    steps: [checkout, setup-uv, uv sync --group core --group dgx --group galaxy --group eval, pytest -q tests/e2e_golden_path_dgx.py tests/e2e_galaxy_constellation.py]
```

## Runner Provisioning Checklist (Phase 6 Explicit Task)

- [ ] Windows ARM64 machine registered as GitHub self-hosted runner (labels: `self-hosted`, `windows`, `arm64`)
- [ ] DGX Spark registered as GitHub self-hosted runner (labels: `self-hosted`, `linux`, `dgx`)
- [ ] Both runners reachable via Tailscale, systemd services running, token rotation configured
- [ ] `windows-tests` and `dgx-integration` jobs flipped to `if: true` in CI

---

*Last hand-updated: 2026-09-18. Not CI-generated — see banner at top.*